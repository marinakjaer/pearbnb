    <%= simple_form_for [@flat, @booking] do |b|%>
    <%= b.label :check_in %>
    <%= b.date_field :check_in, value: params[:arrival] %>
    <%= b.label :check_out %>
    <%= b.date_field :check_out, value:  params[:departure] %>
    <%= b.button :submit, class: 'btn btn-primary' %>
    <% end %>
