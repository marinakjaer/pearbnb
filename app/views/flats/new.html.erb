<h1 class="text-center">Enter information for new flat</h1>

<%= render "shared/new-flat-form" %>

<%= link_to "Go back to search", to: "flats/results?arrival=#{params[:arrival]}&departure=#{params[:departure]}" %>
